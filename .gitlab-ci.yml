default:
  image: docker:stable

variables:
  DOCKER_DRIVER: overlay

services:
  - docker:dind
before_script:
  - apk add --no-cache docker-compose
  - docker-compose up -d
stages:
  - deploy
deploy-job:
  stage: deploy
  script:
    - make bootstrap